apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: platform-demo-tls
  namespace: platform-demo
spec:
  secretName: platform-demo-tls-secret
  issuerRef:
    name: platform-demo-selfsigned-issuer
    kind: Issuer
  dnsNames:
    - app.local
