apiVersion: v1
kind: Service
metadata:
  name: platform-demo-service
  namespace: platform-demo
  labels:
    app: platform-demo
    release: monitoring
spec:
  type: ClusterIP
  selector:
    app: platform-demo
  ports:
    - name: http
      port: 80
      targetPort: 8000
